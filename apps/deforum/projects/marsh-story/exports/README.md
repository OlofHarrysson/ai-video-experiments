# Exports

`e01-v001/preview.mp4` is the 12-second invitation → signal → reveal cut, comprising three four-second shots. `cut.json` records every source frame and hash. The 24 FPS delivery repeats eight generated frames per second without interpolation. The shared local review harness stores sampled decoded frames and timestamp/event metadata in versioned `reviews/` folders beneath the export.
