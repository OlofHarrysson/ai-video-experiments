# Runs

Three unique ComfyUI jobs belong to experiment `story` / E01; `submission.json` identifies the shot. Each collected run preserves its graph, anchor, remote archive, frames and preview. Keep every attempt. Recover an existing job through app-level `experiment.py collect PATH` after an interruption; do not resubmit it.
