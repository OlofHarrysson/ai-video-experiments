# Movie exports

No assembled cut has been exported yet. Save future previews under their cut version, such as `v001/`, along with the cut used to produce them. Keep earlier exports.

The first denoise-comparison preview is in [this project’s exports](2026-09-06-comparison/). An export is separate from the original render attempts and from the cut that selects their ranges.
