# Preserved generations

Shared `serverless_client` creates a unique timestamped directory per submission and retains all collected attempts. Three stills per successful run map to settings in that run's `submission.json`. Keep originals, cloud archives, rejected variants and receipts.

- `20260906T214908281394Z-settings-3f`: job `79f63e28-03a7-4e82-980e-d25e4a385818-e2`, COMPLETED; all three 1024×576 originals and six manifest-listed archive files verified. Visual review in `../experiments/settings.md`. `verified-images.json` maps settings to image hashes; `job-final-status.json` preserves final API timing.
