# Owl transformation at 1.5× speed

Olof says the transformation looks quite nice and requests faster playback with exactly the same frames, since slow playback makes mistakes easier to notice. The assistant selects 1.5× as the first audition; preference for this speed is pending.

- Source: `../exports/owl-transition-v001/owl/cadence-24/interpolated/preview.mp4`.
- Source range: frames `[0, 336)` at 24 fps, fourteen seconds.
- Output: [owl at 1.5×](../exports/owl-transition-speed-v001/owl-1.5x.mp4), all 336 frames at 36 fps, 9⅓ seconds.
- The original stays intact. Generation timing, diffusion, spatial transforms and RIFE are unchanged. This is a playback retime only; the existing one-second repaint interval appears every ⅔ second in the faster video.
- No GPU, new image generation, frame dropping or re-encoding. Both passes copy the existing H264 packets while changing timestamps. All 336 decoded frames match the original in order; uniform 36 fps timestamps and full decode are verified.

[Verification and exact FFmpeg commands](../exports/owl-transition-speed-v001/verification.json) retain source/output hashes and per-frame hashes. An initial timebase-only attempt failed timestamp validation and is preserved as `preflight-timebase.mp4`, excluded from this cut. The final version first scales input timestamps, then normalizes presentation/decode times and packet durations to 512 ticks in a 1/18432 timebase.
