# Workflows

The [upstream reference](../references/README.md) contains the starting graph. An adapted SDXL workflow will be saved here after the first experiment is selected and the actual RunPod node/model schemas are inspected. No unexecuted graph is labeled ready to render.
