# Repository guidance

## Active project

- The [0.5s versus 1s repaint experiment](apps/deforum/projects/modern-model-study/experiments/repaint-intervals.md) is complete: two six-second shots at native 24 fps, cadence 12/24, same three-step Krea recipe, time-based twist/expansion, and RIFE 4.25 finishing. All 16 fresh diffusion calls and 559 archive files are verified locally; owned Pod deleted, retained model cache preserved. **Olof selects the one-second repaint version as better and more morphing. Use 1s (cadence 24) as the creative baseline.** Both have some soft/doubled details during interpolation. The local [RIFE scale comparison](apps/deforum/projects/modern-model-study/experiments/rife-scale.md) is complete with these paintings unchanged: half-scale is the assistant's tentative pick for less doubled detail in later sampled transitions, but the first pair is mixed and human playback judgment is pending. Both are six seconds at full-resolution 24 fps; default RIFE scale remains 1.0. No cloud resource used. The final 0.5s/1s use native warp-only frames. Olof explicitly accepts the preceding 24 fps migration as visually identical. Do not change diffusion strength or RIFE settings while attributing this comparison to repaint interval.

- New feedback experiments use **24 fps working and delivery timelines**, time-based warps, and RIFE finishing by default. Keep cadence visible: the selected one-second repaint interval is cadence 24; the earlier 0.25-second baseline is cadence 6; the previous 7/10/15 at 12 fps become 14/20/30 at 24 fps. `FeedbackTiming(repaint_seconds=1)` in `apps/deforum/feedback_timing.py` expresses the selected pacing; its historical constructor default remains 0.25 seconds; pass it to the recurrent runner's `timing` argument. Its omitted timing argument remains a legacy 12 fps adapter for preserved experiments. Use `warp_at_time(image, start_seconds, end_seconds)`; higher FPS must not increase motion per second. The [24 fps migration check](apps/deforum/projects/modern-model-study/experiments/timebase24.md) replays recorded diffusion responses, validates identical repaint inputs, and reuses the exact existing RIFE frames. This is local equivalence evidence, not fresh GPU inference or a visual-quality improvement. Default deliveries use interpolation; retain raw frames for diagnosis and use native 24 fps warps after the final anchor. No extra per-frame warping is applied to RIFE output.

- Olof finds [RIFE substantially better and cadence 2 worse](apps/deforum/projects/modern-model-study/experiments/turbo-smoothing.md). That earlier baseline used three native Turbo sampling intervals, cadence 3 at 12 fps, and RIFE finishing; the selected one-second pacing above supersedes its interval. Keep raw versions for diagnosis; interpolation never feeds back into generation. The [cadence 3/7/10/15 study](apps/deforum/projects/modern-model-study/experiments/cadence-slow.md) is complete both raw and with the same local RIFE settings. No new diffusion was needed for finishing. All anchors retain their original timestamps; delivery is three seconds at 24 fps. The final short interval retains warp-only motion because no later anchor exists. RIFE spreads redraw transitions, with some doubled/softened fine details. Human playback verdict is pending; 10 remains an assistant audition candidate, not a selected winner. Earlier [4/5 RIFE comparisons](apps/deforum/projects/modern-model-study/experiments/cadence-spacing.md) are also preserved. Describe pacing using seconds per repaint, source-motion FPS and delivery FPS: `seconds/repaint = cadence / source-motion fps`; duplicated delivery frames do not change this rhythm. Olof's too-busy/too-uneventful thresholds remain hypotheses. All results verified locally; no Pod used in the interpolation follow-up. Authorized model cache remains.

- Latest: [ten-second transitions and Turbo tails](apps/deforum/projects/modern-model-study/experiments/turbo-transitions.md). Olof accepts the earlier single-final-step improvement and requested this longer shot plus final-two/final-three tests. The ten-second one-step twist/expansion still flattens. Three-step short continuation restores richer shading/architecture with larger redraws; two is milder. Olof calls this the best result so far and prefers three steps for its richer detail, while reporting too much flicker. Preserve it as the current creative baseline and prioritize smooth morphing between repaints without losing detail or suppressing interesting structural evolution. Native tails start at different noise levels; this is not a fixed-noise step-count test. All 61 images/124 remote files are local and verified; both owned Pods deleted. The separate persistent-volume trial owns other active cloud resources; do not claim account-wide zero spend or delete those resources. Olof authorizes assistant-led experiment choices.
- Olof selects **Lanczos** for future RGB warps. Use `apps/deforum/spatial_warp.py` (`remap_rgb`, Lanczos4, reflected border); preserve historical bilinear controls. [Latent feedback](apps/deforum/projects/modern-model-study/experiments/latent-feedback.md) still flattens without repeated encoding, and Olof sees little difference from RGB. Defer latent-motion integration; distillation remains an unproven cause.

- Latest diagnosis: [repeated repainting](apps/deforum/projects/modern-model-study/experiments/repaint-diagnosis.md) confirms no-motion Krea quality drift over 24 cycles; repeated VAE reconstruction adds artifacts even without intermediate PNG feedback. Bilinear warps independently blur detail. Lanczos improves motion-only detail but only partly improves the full animation. The subsequent latent control is complete (see above). Preserve feedback and do not promise latent warping is equivalent to RGB warping. All 84 jobs / 173 remote files are local and verified, owned Pod/storage deleted.

- Current direction: **Krea only**. Olof finds the cathedral clips still too flickery and requests repaint strengths below 0.30. [Lower repaint study](apps/deforum/projects/modern-model-study/experiments/krea-low-repaint.md) completed 0.10/0.18/0.24 with the same opening, twist and cadence. Assistant first-pass pick is 0.10 for smaller redraws and better retention of fine structure; Olof’s playback choice is pending. All 33 repaints and 68 remote files are local and verified; Pod/storage deleted, spend rate zero. Preserve Klein results as history; pause new Klein renders.

AI Video Experiments is a public notebook and workspace for controllable AI animation. The creative interest is explicit camera movement combined with surreal morphing and visible frame-by-frame repainting. Perfect temporal consistency is not the goal.

- Current experiment: `apps/deforum/`. Latest loops use local spatial warps/cadence with native modern-model reference conditioning or earlier ComfyUI img2img on RunPod; earlier Difforum/depth-node experiments are preserved.
- Earlier animation comparison: [cathedral feedback](apps/deforum/projects/modern-model-study/experiments/cathedral-feedback.md) completed four three-second clips: two repaint strengths per model, unchanged cathedral openings, full-resolution twist, cadence 3 and no reference conditioning/blending/RIFE. Assistant candidate is Krea denoise 0.45 for clearer rebuilding; 0.30 softens, while both Klein partial-noise recipes accumulate grain. **Olof selects Krea but finds repainting too flickery; the lower-strength follow-up is above.** All 44 generated images and 90 remote files are local and verified; owned Pod/storage deleted.
- Latest opening audition: [seven stills](apps/deforum/projects/modern-model-study/experiments/opening-art.md) complete: official Krea control plus three concepts per model. Normal FP8 Krea without a reference LoRA renders cleanly; the assistant recommends its living cathedral, with Klein’s darker cathedral as an alternative. **Olof likes both cathedral openings and subsequently selects Krea for further animation.** No animation tested in this round; preserve same-model warped-image initialization when resuming feedback. All outputs are local, owned Pod/storage deleted, final spend rate zero.
- Latest modern-model study: [initialized feedback plus optional reference](apps/deforum/projects/modern-model-study/experiments/additive_reference.md) completed 34 images, four clips and two comparisons with verified feedback lineage. All media is local and owned cloud resources are deleted. **Olof’s 2026-09-09 feedback:** Klein has better consistency in some respects but is less interesting than the SDXL/reference Deforum art; he rejects Krea including its opening. Do not treat the assistant’s earlier Klein continuation recommendation as an accepted creative direction. Prioritize compelling opening artwork and expressive evolution over continuity alone. The latest Krea opening used a style-reference LoRA without an image, an unvalidated control; model capability versus recipe quality remains unresolved. See the [quality diagnosis](apps/deforum/projects/modern-model-study/experiments/additive_reference.md#quality-diagnosis-and-human-feedback--2026-09-09).
- Current priority: establish a usable iterative filmmaking workflow, then expressive spatial image motion and quality improvements. Camera/depth transforms and regional deformation are possible techniques. Build toward 30-second, one-minute and five-minute assemblies from manageable clips; avoid a one-shot long render.
- Current visual task: [Brain Entity reproduction](apps/deforum/projects/brain-entity-study/README.md). **P3 + RIFE** is the creative baseline: Olof likes its morphing and wants more interesting camera movement. SDXL is acceptable for now, with a newer-model audition wanted later. C01/C03 were boring; avoid overwhelming Olof with similar variants. [RIFE finishing](apps/deforum/projects/brain-entity-study/experiments/rife-results.md) runs locally on MPS in about 16 seconds per six-second clip; interpolated noise during generation remains untested.
- Next reference: [The BonsAi Effect](docs/research/bonsai-effect-workflow.md), especially the supplied Evolve Zoom Slow chapter. Both videos are archived and visually sampled; creator replies, original motion presets and hybrid guide videos are preserved. These are WebUI presets, not importable ComfyUI graphs. Keep guide-driven motion, seed mixing, camera/depth warp and final interpolation distinct when adapting them.
- Current research direction: Olof identifies Safety Marc's presets as central evidence for successful spatial image controls. [Spatial-control study](docs/research/bonsai-spatial-motion.md) inventories all 95 files and maps camera schedules, guide flow and guide compositing. The accepted [Move-Warp-inspired test](apps/deforum/projects/motion-guide-study/experiments/move-warp.md) now applies the actual wave guide before every B-recipe repaint, orchestrated on the Mac using existing ComfyUI img2img nodes. Six-second repaint and warp-only videos are local; the flame develops into a mechanical head. Olof says this looks much better and the movement is more interesting, without endorsing the particular motion. The applied flow has a measured rightward bias; no separate pan was added. Owned cloud resources are cleaned up.
- [Three spatial effects](apps/deforum/projects/motion-guide-study/experiments/motion-effects.md): Olof selects radial unfolding as by far the best. He values the 3D movement and subject repositioning, but rejects its large black empty area. [Round two](apps/deforum/projects/motion-guide-study/experiments/motion-effects-2.md) adds fourfold ring expansion, turbulent warping and rotating unfolding; all three clips are complete, archived locally and awaiting his playback feedback. Stronger warps sometimes introduce another figure. Matched mask-repair/prompt probes barely improve the black band; gap handling remains unresolved. Owned compute/storage is cleaned up. Use the [motion vocabulary](docs/research/motion-control-vocabulary.md) to connect descriptions to controls and previews. Wave diagnosis distinguishes travelling ripples from bounded checker junctions; the 98% statistic describes estimated flow, not tracked material motion.
- Stay with ComfyUI; Olof deferred A1111/Forge. The [motion-guide walkthrough](apps/deforum/projects/motion-guide-study/README.md) shows guide pairs, estimated arrows, local pixel warps and six matched repaint samples. The subsequent Move-Warp test completes a Mac-orchestrated guide-flow feedback loop. It does not reproduce the full preset's camera, depth, seed mixtures or compositing, and it checkpoints each image through an 8-bit PNG. Warm per-frame job execution is about 2.4 seconds; total frame round trips about 7–8 seconds.
- [Same-model seed comparison](apps/deforum/projects/motion-guide-study/experiments/seed-comparison.md) complete: Olof rejected A's fixed seed and selected B's changing seed. Keep incrementing seeds for simple reproducibility. In the [small 3D follow-up](apps/deforum/projects/motion-guide-study/experiments/seed-3d.md), Olof barely perceived movement in the repaint and found the visible guide motion too weak. He now questions whether literal 3D describes the goal; prioritize expressive regional motion. The two new BonsAi films are archived and selectively reviewed. Media is local and owned cloud resources cleaned up.
- Compute choice: Olof authorizes short-lived ComfyUI Pods now and for future experiments whenever they are more convenient. Prioritize iteration speed over serverless; model downloads still need verification. The small prepaid balance is acceptable learning risk. Download results promptly and clean up owned compute/storage when finished.
- Mac: authoring, controls, previews, and local copies of results. RunPod: model inference and the camera-warp/img2img feedback loop.
- First milestone complete: three five-second SDXL clips with the same initial image, seed sequence, and camera path at different denoise strengths. Outputs are local; the experiment Pod was deleted.
- Runnable path: `uv run python experiment.py init-project NAME`, then `run --project NAME --experiment EXPERIMENT ...` in `apps/deforum/`. Write the experiment note before rendering. Serverless is verified: continuation, depth guide and 3D repaint completed. Its endpoint is paused (min/max workers 0); the archive volume was deleted after verified local downloads. See `apps/deforum/serverless/README.md`. MCP endpoint listing is now verified; the CLI also works.
- Seed finding: incrementing the feedback seed retained more detail than a fixed seed in the first short test. Review full-length quality in the session report before choosing the next recipe.
- Playback feedback: 0.30 is too smooth; 0.50 flickers too much. Frame sharpness alone does not establish video quality. Keep detail, flicker, structure and camera control separate in reviews.
- Current practice cuts: v001 preserves parent frames 0–19 and selects a 2D continuation; v002 selects a 3D continuation from the same anchor. Review playback before choosing the next branch. Depth-guide parallax is measured; 3D repaint has edge artifacts. Current 3D feedback reuses initial depth through the loop and does not establish long-shot scene consistency.
- Budget: Olof authorized using the remaining roughly $50 RunPod balance for parallel learning. Multiple workers are allowed; the latest round used a maximum of three. Check current account state before provisioning and clean up owned resources afterward.
- Long-term recurring cost should remain near SDXL, but the current learning budget permits model/workflow exploration. Separate warm inference cost from setup/idle time and rejected attempts.
- Iteration speed matters: the expanded worker took 19 minutes to build and 6.5 minutes for its first cold start, versus 13 seconds for three warm stills and about three minutes per six-second feedback clip. Reuse a prepared image across settings/workflow tests; those changes require no rebuild. Uncached workers can still incur setup delay. Keep a bounded warm session for review, then pause and clean up. The Dockerfile removes the intermediate QR model within the download layer; that packaging fix was not used in the first study runs.
- Earlier: [ten video experiments](docs/research/ten-experiments-session.md) completed. Fixed-seed independent SDXL redraw is a candidate for preserving the scene; masked repairs produce interesting but inaccurate morphs. Native Seedream per-frame editing works with inline image data, but introduces variable canvas borders. Feedback still progressively softens. These are sampled-frame findings; Olof's playback preferences remain unconfirmed. All resources are cleaned up and media is local.

## Working conventions

- Keep the authorized 50 GB standard `deforum-models` network volume in EU-RO-1 between sessions ($3.50/month). Use [the Pod workflow](apps/deforum/POD.md): existing model cache and ComfyUI runtime are reused; delete completed owned Pods, not this retained volume. Check GPU capacity before allocation and preserve explicit warm-Pod handoffs between experiments.

- Keep the Deforum feedback loop intact when changing models or prompt inputs. Olof's modern-model request means warped previous-image sampling initialization plus text, optionally adding native reference-image conditioning. “Text-to-image” does not authorize independent redraws or removing that initialization. Explain initialization and reference conditioning separately, verify that both can be used together, and align before changing the loop. See the [misunderstanding record](docs/review-and-feedback.md#misunderstanding-record--2026-09-08).
- Use the central `log-agent-friction` skill for consequential misunderstandings, wrong assumptions, poor outputs and workflow/tool problems while evidence is fresh. Keep project-specific prevention rules and experiment corrections here alongside the central incident record.

- Earlier modern-model audition: Olof finds the sampler changes small and wants newer models with model-specific prompting guidance. The [workflow audit](docs/research/modern-model-transition.md) confirms the latest graph has nine core ComfyUI nodes and no Difforum dependency; two legacy packs remain installed but unused. Olof approved a bounded Klein 4B distilled and Krea 2 Turbo audition. Each model must create its own opening and feedback, with model-specific prompts; next-frame instructions are a hypothesis. The [new project](apps/deforum/projects/modern-model-study/experiments/baseline.md) owns execution. The serverless attempt stopped before inference. The approved [ComfyUI Pod retry](apps/deforum/projects/modern-model-study/experiments/pod-results.md) completed 32 images and two three-second clips; all media is local and the Pod is deleted. Krea’s scene-description recipe was the assistant’s initial candidate: steadier palette and expressive redraw, but cadence ghosting and partial resistance to warps. Klein native editing preserves layout but accumulates harsh colors. Krea’s instruction-worded img2img probe removed the subject; do not treat it as native editing. Olof finds both interesting but reports Krea jitter and doubled frames; no winner selected. The active worker Dockerfile packages these models using core nodes; the original is preserved as `Dockerfile.sdxl`. Reuse motion/cadence/review tooling. Follow [prompting notes](docs/research/prompting-for-feedback.md), archive any prompt expansion, and test warp retention before a full clip.
- Latest sampler study: [two Euler variants](apps/deforum/projects/motion-guide-study/experiments/samplers.md) completed at the same 0.45/18-step/CFG4.5/Karras recipe. Ordinary Euler is closer to DPM++ 2M; ancestral changes more structure and texture. Retain the current control pending Olof’s playback feedback. [Model/sampler research](docs/research/model-sampler-recipes.md) records exact Civitai versions, recommended settings and source disagreements. Consult the exact model recipe before changing families; distilled checkpoints can need different samplers, schedules and guidance. More-art recommends strength 0.8–1.0 versus our 1.1; defer that separate test. Outputs are local, workers paused and temporary volume deleted.
- Current step: [repaint controls](apps/deforum/projects/motion-guide-study/experiments/repaint-controls.md) compares the same three-second twist at denoise 0.35/0.45/0.58 and adds one 0.35 branch with bottom-left sampler protection and top-right image noise. Olof selects **0.45**, finding 0.35 blurry and 0.58 too changeable. He rejects protected-region artifacts. The [noise/steps test](apps/deforum/projects/motion-guide-study/experiments/noise-steps.md) compares 18/36 steps at 0.45 with/without top-right noise and no protection mask; all five clips are local. More steps did not convincingly remove grain; half the added noise at 36 steps is the assistant's noisy candidate, with playback feedback pending. Both temporary volumes are deleted and compute is paused. Olof accepts our two-endpoint warp/blend cadence and values motion-only previews. Keep cadence 3, defer RIFE unless useful, and keep explicit feedback blending, protected-pixel copyback, tracking, guide flow and depth out of this round. Twist reads best; the black border is lower priority. The [22-effect browser](apps/deforum/projects/motion-guide-study/experiments/motion-catalog.md) remains the motion selection tool. Define effects independently of preview artwork; changing a region's center is a parameter change, not an object-named effect.

- Start with the [documentation index](docs/index.md) and the relevant app README. Shared explanations belong in `docs/`; runnable experiments belong in `apps/`.
- Prefer existing nodes and workflows. Add custom code only for a concrete missing capability.
- Use the repository's RunPod skills in `.agents/skills/` for RunPod work. Keep RunPod skills and MCP configuration scoped here; the CLI may be installed globally.
- Keep credentials in ignored local files or the credential store. Never commit keys, OAuth tokens, or private account responses.
- Inspect Git status and preserve unrelated work. Keep changes on the existing branch unless asked otherwise.
- Record workflow/model versions, settings, and outputs for each experiment. Distinguish source inspection, local checks, remote execution, and visual review.
- Treat learning filmmaking/tooling and improving human–agent collaboration as explicit project outcomes. Follow the living [review and feedback agreement](docs/review-and-feedback.md), update it from experience, and never infer understanding just because an explanation was shown.
- Do the first-pass review before asking Olof to judge taste. Default to one recommendation and one meaningful alternative, shown as playable videos in chat; preserve and index every result, and show the full set when requested. Explain what changed, why, what to watch for and what happened in plain language. Define unfamiliar tools such as RIFE; IDs/node names alone are insufficient. Human playback preferences take precedence over assistant rankings.
- Inspect clips with `apps/deforum/video_review.py`: evenly spaced overview, selected every-frame windows, then timestamp-matched comparisons. Use small pages and a bounded image budget; open full-size frames only where useful. Keep artifacts beside project media. Pixel-change candidates locate intervals, not semantic events; sampled frames alone do not establish playback quality. See [local video review](docs/video-review.md).
- Consult and maintain [Olof's learning journal](docs/olof-learning-journal.md) when preferences or learning affect the next step. Separate explicit preferences, demonstrated concepts and unconfirmed exposure; never infer understanding from merely presenting information. The Seedream marsh still was the earlier model-comparison preference; the supplied art references set the current target.
- Preserve every generation, original reference and cut version. All project media belongs under `projects/PROJECT/`, including the first session. Shared runner, setup code and reusable workflow recipes stay at app level. A working cut references source ranges and never overwrites a generation. Git ignores media; no separate-disk backup is configured yet.
- Keep the product vision and research living. Defer automatic editing/branching infrastructure and the simple-3D-scene-to-stylization hypothesis until short experiments justify them.
- Check for an existing user-created Pod before provisioning. Track the resources owned by this experiment and shut down paid compute when finished; retained storage can still cost money.

## Documentation

- [Documentation index](docs/index.md): canonical map of shared notes and app runbooks.
- [Review and feedback agreement](docs/review-and-feedback.md): assistant screening, a small human shortlist, plain-language experiment explanations and iterative collaboration.
- [Feedback parameter research](docs/research/feedback-parameters.md): pinned node behavior, explicit distinctions between image noise, denoise, sampling steps and previous-frame influence.
- [Filmmaking guide](docs/research/filmmaking-for-ai-animation.md): image-led storytelling, directing, composition, lighting, camera craft, editing and later music.
- [Olof's learning journal](docs/olof-learning-journal.md): evidenced preferences, demonstrated concepts and open learning questions.
- [Creative direction](docs/vision.md) and [working convention](docs/workflow.md).
- [Project index](apps/deforum/projects/README.md): project briefs, experiments and current cuts.
- [Deforum mechanism and creative direction](docs/deforum.md).
- [RunPod tools and repository setup](docs/runpod.md).
- [Deforum experiment](apps/deforum/README.md): current state and next execution steps.
- [Parallel experiment results](docs/research/parallel-experiments-session.md): model samples, settings, overscan and independent redraw.
- [Serverless session results](apps/deforum/projects/botanical-cathedral/experiments/serverless-results.md): cuts, parallax, startup failure, cached timings and cleanup.
- [First session results](apps/deforum/projects/botanical-cathedral/experiments/baseline-results.md): execution, visual findings, and cost.
